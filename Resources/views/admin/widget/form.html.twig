{{ form_start(form) }}

	{% block body %}
		{{ form_widget(form) }}
	{% endblock body %}

	{% block footer %}
	    <div class="ln_solid"></div>

	    <div class="form-group">
	        <div class="col-md-6 col-sm-6 col-xs-12 col-md-offset-3">
	        	{% block buttons %}
		            <button type="submit" class="btn btn-info">
		                <i class="fa fa-save"></i> {{ 'submit'|trans({}, 'admin') }}
		            </button>
		        {% endblock buttons %}
	        </div>
	    </div>
	{% endblock footer %}

{{ form_end(form) }}